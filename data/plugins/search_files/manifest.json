{
    "name": "Search Files",
    "id": "search_files",
    "version": "1.0.0",
    "description": "Searches for files by partial name match and returns their paths.",
    "author": "Genesis",
    "maker": "Computer Aces",
    "actions": [
        {
            "name": "search_files",
            "description": "Finds files matching the query string in the filename.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "Regex pattern to search for in filenames"
                    },
                    "path": {
                        "type": "string",
                        "description": "Root directory to search in. Use '/' or '\\\\' to search ALL available drives on Windows."
                    }
                },
                "required": [
                    "query"
                ]
            },
            "script": "main.py",
            "trigger": "manual"
        }
    ]
}